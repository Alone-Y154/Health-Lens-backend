# HealthLens Server - Docker Environment Variables
# Copy this file and set OPENAI_API_KEY before running docker-compose

# OpenAI API Key (REQUIRED)
# Get your key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk_your_openai_api_key_here

# Node Environment
NODE_ENV=production

# Server Port (Docker container exposes on port 3000)
PORT=3000
